# 클로드 코드용 단계별 개발 프롬프트 목록

카드혜택 계산기(가칭) — 위 PRD.md를 프로젝트 루트에 함께 두고, 아래 프롬프트를 순서대로 클로드 코드에 붙여넣어 진행하세요. 각 단계는 이전 단계의 결과물을 전제로 합니다. 단계 사이마다 결과를 확인하고 다음으로 넘어가는 것을 권장합니다.

---

## Step 0. 프로젝트 초기화

```
첨부된 PRD.md를 읽고 이 프로젝트의 목표를 이해해줘.
Next.js(TypeScript) 기반으로 새 프로젝트를 초기화하고, 아래 기준으로 기본 폴더 구조를 잡아줘.

- src/app: 라우팅 (홈, 카드선택, 소비패턴입력, 결과)
- src/features: 도메인별 로직 (cards, calculator, subscriptions)
- src/lib: 공용 유틸
- data: 카드/혜택 시드 데이터를 담을 위치

상태관리는 Zustand를 사용하고, 로컬 스토리지 연동을 고려한 스토어 구조를 미리 잡아줘.
아직 실제 카드 데이터나 계산 로직은 구현하지 말고, 뼈대와 네비게이션만 만들어줘.
```

---

## Step 1. 데이터 모델 설계 및 시드 데이터

```
PRD.md의 "7. 데이터 모델"과 "8. 혜택 계산 로직 상세"를 참고해서 TypeScript 타입을 정의해줘.
- Card, BenefitRule, SubscriptionService, UserSelection 타입을 src/features/cards/types.ts 등에 정의
- 전월실적 조건, 카테고리별 한도, 통합 한도까지 표현할 수 있는 구조로 설계해줘

이후 실제 크롤링 전 개발/테스트용으로, 국내 주요 카드사 카드 5~7개 정도의 샘플 시드 데이터를 data/cards.seed.json에 직접 작성해줘.
각 카드는 최소 2~3개 카테고리 혜택, 전월실적 조건, 카테고리별/통합 한도를 포함한 현실적인 예시로 만들어줘 (실제 카드사 공식 페이지 내용을 참고해서 최대한 정확하게).
```

---

## Step 2. 공식 데이터/크롤링 파이프라인 조사 및 골격 구축

```
PRD.md의 "9. 데이터 수집 전략 및 리스크"를 참고해서 진행해줘.

1. 먼저 금융감독원, 여신금융협회, 공공데이터포털 등에 카드 혜택 관련 공식 API/공개 데이터가 있는지 조사하고 결과를 요약해줘.
2. 공식 데이터로 충분히 커버되지 않는 항목이 있다면, 어떤 카드사 공식 홈페이지를 크롤링 대상으로 삼을지 후보를 정리해줘.
3. 실제 크롤링 스크립트를 바로 구현하기 전에, 대상 사이트의 robots.txt와 이용약관을 확인해서 크롤링 가능 여부를 알려줘.
4. 문제가 없는 범위에서, scripts/crawl-cards.ts (또는 Python) 형태로 크롤러의 기본 골격(요청 빈도 제한 포함)을 만들어줘. 이 단계에서는 실제 대량 수집은 하지 말고 소수 카드로 동작 검증만 해줘.

결과는 Step 1의 데이터 모델(Card, BenefitRule)에 맞춰 저장되도록 해줘.
```

---

## Step 3. 혜택 계산 엔진 구현

```
PRD.md "8. 혜택 계산 로직 상세"를 정확히 구현해줘. src/features/calculator에 순수 함수 형태로 작성해줘.

계산 순서:
1. 사용자 카테고리별 지출 합계로 카드별 전월실적 충족 여부 판정
2. 미충족 시 카드별 정의된 조건에 따라 혜택 미적용/조건부 적용 처리
3. 카테고리별 지출 × 할인율/적립률로 혜택액 산출 후 카테고리별 한도 절삭
4. 카테고리별 혜택 합산 후 통합 한도 재적용
5. 연회비를 차감한 순혜택 계산
6. 계산 근거(전월실적 충족 여부, 한도 소진율 등)를 함께 반환하는 구조로 만들어줘

Step 1의 시드 데이터를 입력값으로 하는 단위 테스트(vitest 또는 jest)를 여러 케이스(실적 미충족, 카테고리 한도 초과, 통합 한도 초과 등)로 작성해서 로직이 정확한지 검증해줘.
```

---

## Step 4. 카드 선택 UI

```
카드 검색/선택 화면을 만들어줘.
- Step 1의 시드 데이터(data/cards.seed.json)를 사용
- 카드사/카드명 검색, 카드 종류(신용/체크) 필터
- 카드를 선택하면 "내 카드" 목록에 추가/제거 가능
- 선택 상태는 Zustand 스토어를 통해 로컬 스토리지에 저장되어 새로고침해도 유지되게 해줘

디자인은 아직 최소한으로만 (기본 스타일), 다음 단계에서 디자인을 다듬을 예정이야.
```

---

## Step 5. 소비 패턴 입력 UI

```
소비 패턴 입력 화면을 만들어줘.
- PRD의 카테고리 목록(외식/카페, 통신, 교통, 온라인쇼핑, 마트/편의점, 구독/OTT 등)별 예상 지출 금액 입력 폼
- 구독 서비스 사전 정의 목록(넷플릭스, 유튜브 프리미엄 등)을 체크박스로 선택하는 UI
- 입력값도 로컬 스토리지에 저장되어 유지되게 해줘
- 구독 서비스 선택 시 해당 카테고리 지출 금액에 자동 합산되는 로직도 함께 넣어줘 (PRD 4.1의 "둘 다 입력받는" 요구사항 반영)
```

---

## Step 6. 결과 화면 (비교표 + 총 혜택 금액)

```
Step 3의 계산 엔진과 Step 4/5에서 저장된 사용자 데이터를 연결해서 결과 화면을 만들어줘.

- 내가 선택한 카드들의 혜택 비교표: 행은 카드, 열은 카테고리별 혜택/전월실적 충족 여부/한도 소진율/순혜택
- 카드별 총 혜택 금액(연회비 차감 후 순혜택) 요약 카드, 순혜택 기준 정렬 가능하게
- 결과 화면 하단에 "실제 카드사 조건과 다를 수 있습니다" 안내 문구 표시 (PRD 9번 항목)
```

---

## Step 7. 디자인 시스템 적용

```
뱅크샐러드/토스 스타일을 참고해서 전체 UI를 다듬어줘.
- 단순하고 신뢰감 있는 금융 서비스 톤앤매너 (여백감, 카드형 레이아웃, 명확한 색상 대비)
- 결과 비교표는 숫자 정보가 많으니 가독성을 최우선으로 (정렬, 색상으로 긍정/부정 구분 등)
- 공통 컴포넌트(버튼, 인풋, 카드, 테이블)를 재사용 가능한 형태로 정리해줘

Tailwind CSS를 사용하고, 별도 UI 라이브러리가 필요하면 제안해줘.
```

---

## Step 8. QA 및 엣지 케이스 점검

```
전체 플로우를 다시 점검해줘.
- 카드를 하나도 선택하지 않은 상태, 소비 금액을 하나도 입력하지 않은 상태 등 빈 입력 케이스 처리
- 전월실적 조건이 없는 카드, 한도가 없는 카드 등 데이터 예외 케이스 처리
- 로컬 스토리지 데이터가 깨졌거나 없을 때의 안전한 폴백 처리
- 데스크톱 기준 주요 해상도에서 레이아웃 깨짐 없는지 확인

발견된 이슈를 목록으로 정리하고 수정해줘.
```

---

## Step 9. 배포

```
Vercel 배포를 위한 설정을 확인하고 배포 가능한 상태로 만들어줘.
- 환경변수가 필요한 부분이 있다면 정리
- README에 프로젝트 개요, 실행 방법, 데이터 갱신(크롤러 실행) 방법을 문서화해줘
```

---

## 참고: 이후 로드맵 (v2, 별도 프롬프트로 진행 권장)

- 계정 기반 로그인 및 서버 저장으로 전환 (로컬 스토리지 → 계정 마이그레이션)
- 카드 추천 순위 기능 추가
- 관리자 대시보드(카드/혜택 데이터 CRUD) 구축
- 크롤링 자동 스케줄링
- 수익 모델 적용

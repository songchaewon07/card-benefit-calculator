# 카드혜택 계산기 (가칭) — 제품 요구사항 문서 (PRD)

버전: v1.0 (초안) · 작성일: 2026-08-19

---

## 1. 개요

### 1.1 한 줄 정의
사용자가 보유한 신용/체크카드와 자신의 소비 패턴(카테고리별 지출, 구독 서비스)을 입력하면, 카드별 혜택 금액과 총 혜택 금액을 정밀하게 계산해 비교표로 보여주는 웹 도구.

### 1.2 배경 및 목적
- 카드사별 혜택(할인율, 적립률, 전월실적 조건, 한도)이 복잡하고 흩어져 있어, 사용자가 자신에게 유리한 카드를 파악하기 어려움.
- 기존 서비스(카드고릴라, 뱅크샐러드 등)는 카드 추천/비교에 초점이 있으나, 본 서비스는 **"내 실제 소비패턴 입력 → 정밀한 혜택 금액 계산"**에 집중.
- 회원가입 없이 즉시 사용 가능한 가벼운 계산기로 시작해, 이후 계정 기반 서비스로 확장.

### 1.3 서비스 카테고리
SaaS형 웹 도구 (B2C, 업무/생활 효율화)

---

## 2. 목표 및 성공 지표 (MVP 기준)

| 목표 | 지표 |
|---|---|
| 정확한 혜택 계산 제공 | 전월실적 조건·카테고리별/통합 한도를 반영한 계산 오차 최소화 |
| 즉시 사용 가능한 UX | 로그인 없이 첫 화면 진입 후 3분 내 결과 확인 |
| 신뢰할 수 있는 카드 데이터 | 국내 주요 카드사 카드 커버리지, 데이터 최신화 프로세스 확보 |

---

## 3. 타겟 사용자

- **주 타겟**: 일반 소비자(B2C), 특정 직업군 제한 없음
- **페르소나 예시**: "여러 장의 카드를 쓰고 있는데 어떤 카드가 내 소비패턴에 가장 유리한지 헷갈리는 20~40대 직장인"
- 로그인 장벽 없이 가볍게 써보고 싶은 사용자를 우선 고려 (첫 방문 전환율 중요)

---

## 4. 핵심 기능 (Feature Scope)

### 4.1 MVP 범위 (v1)

1. **카드 데이터베이스**
   - 국내 주요 카드사의 카드 목록 및 혜택 정보(연회비, 카테고리별 할인/적립률, 전월실적 조건, 카테고리별 한도, 통합 한도)
   - 데이터 소스: 공식 API/공공데이터 우선 확인 → 부족한 항목만 크롤링으로 보완
   - 갱신 방식: 필요 시 수동 실행 (자동 스케줄링은 v1 범위 아님)

2. **내 카드 등록**
   - 로그인 없이 카드 목록에서 검색/선택하여 "내 카드"로 등록
   - 등록 정보는 **브라우저 로컬 스토리지**에 저장 (기기별 유지)

3. **소비 패턴 입력**
   - 카테고리별 예상 지출 금액 직접 입력 (예: 외식비, 통신비, 교통비, 마트/온라인쇼핑 등)
   - 구독 서비스 목록에서 체크박스로 선택 (예: 넷플릭스, 유튜브 프리미엄, 통신 요금제 등 사전 정의 목록)

4. **혜택 계산 엔진 (정밀 계산)**
   - 카테고리별 할인율/적립률 적용
   - **전월실적 조건** 충족 여부 반영 (미충족 시 혜택 미적용 또는 조건부 적용)
   - **카테고리별 한도** 및 **통합(전체) 한도** 반영한 실수령 혜택액 산출
   - 연회비 차감한 순혜택(net benefit) 계산

5. **결과 화면**
   - 내가 등록한 카드들의 혜택 비교표 (카테고리/조건별로 항목화)
   - 카드별 총 혜택 금액(연회비 차감 후 순혜택) 계산 결과

### 4.2 v1 제외 범위 (Out of Scope, 추후 로드맵)

- 관리자 대시보드(카드/혜택 데이터 CRUD UI) — v1은 관리자 기능 없이 데이터 파이프라인 산출물을 직접 반영
- 계정 기반 로그인/회원가입, 기기 간 동기화
- 카드 추천 순위 기능 (v1은 "비교"와 "계산"까지만, 추천 알고리즘은 후속 버전)
- 수익 모델(구독/광고/제휴 등) — 추후 결정
- 실시간 자동 크롤링 스케줄링
- 모바일 최적화 (v1은 데스크톱 웹 우선, 반응형은 후속 보강)

---

## 5. 사용자 플로우

```
[진입] → [카드 검색/선택하여 "내 카드" 등록 (여러 장 가능)]
       → [소비 패턴 입력: 카테고리별 금액 + 구독 서비스 체크]
       → [계산 실행]
       → [결과: 카드별 혜택 비교표 + 총 혜택 금액]
       → [(선택) 카드 추가/수정 후 재계산]
```

- 등록된 카드/입력값은 로컬 스토리지에 저장되어 재방문 시 유지.

---

## 6. 정보 구조 (화면 구성)

1. **홈/랜딩**: 서비스 소개 + "내 카드 등록하기" CTA
2. **카드 선택 화면**: 카드사/카드명 검색, 카테고리(신용/체크, 카드사별) 필터, 선택 카드 목록
3. **소비 패턴 입력 화면**: 카테고리별 금액 입력 폼 + 구독 서비스 체크리스트
4. **결과 화면**:
   - 카드 비교표 (행: 카드, 열: 카테고리별 혜택 + 전월실적 충족 여부 + 한도 소진율 + 순혜택)
   - 카드별 총 혜택 금액 요약 카드(정렬 가능)
5. **(v1 옵션) 내 카드/입력 수정**: 저장된 로컬 데이터 편집

---

## 7. 데이터 모델 (초안)

### Card (카드)
- id, 카드사명, 카드명, 카드 종류(신용/체크), 연회비, 카드 이미지 URL, 전월실적 기준금액, 데이터 출처/최종 갱신일

### BenefitRule (혜택 규칙)
- card_id, 적용 카테고리(외식/통신/교통/온라인쇼핑/구독 등), 혜택 유형(할인율/적립률/정액할인), 값(%, 원), 카테고리별 월 한도, 통합 월 한도, 전월실적 구간별 차등 조건(있는 경우)

### SubscriptionService (구독 서비스 사전 정의 목록)
- id, 서비스명, 매핑 카테고리(예: 넷플릭스 → OTT/구독)

### UserSelection (로컬 저장 데이터, 서버 저장 아님)
- 선택한 카드 ID 목록
- 카테고리별 입력 금액
- 선택한 구독 서비스 목록

---

## 8. 혜택 계산 로직 상세 (정밀 계산 요구사항)

1. 사용자의 카테고리별 지출 합계로 **전월실적 충족 여부** 판정 (카드별 기준금액 대비)
2. 전월실적 미충족 시: 혜택 미적용 또는 카드별 정의된 조건부 규칙 적용
3. 카테고리별 지출 금액 × 할인율/적립률 → 카테고리별 혜택액 산출
4. **카테고리별 한도** 초과분은 절삭(cap)
5. 카테고리별 혜택액 합산 후 **통합(전체) 한도** 재적용
6. 최종 혜택액 − 연회비(월할 또는 연 단위 명시) = **순혜택**
7. 계산 결과에는 어떤 조건(전월실적 충족/한도 소진 등)이 적용되었는지 근거를 함께 표기 (신뢰성 확보)

> 참고: 실제 카드사 약관은 매우 세분화되어 있으므로, v1은 "카테고리 실적 인정 여부, 전월실적 구간, 카테고리별·통합 한도"까지는 반영하되, 그 이상의 예외 조항(특정 가맹점 제외 등)은 데이터 스키마상 `비고` 필드로 남기고 계산에는 단순화하여 반영.

---

## 9. 데이터 수집 전략 및 리스크

- **1순위**: 금융감독원, 여신금융협회, 공공데이터포털 등 공식 API/공개 데이터 확인
- **2순위**: 공식 데이터로 커버되지 않는 항목만 카드사 공식 홈페이지 크롤링으로 보완
- **법적 고려사항**: 크롤링 시 각 카드사 이용약관 및 robots.txt 확인, 과도한 요청으로 인한 차단 방지(요청 빈도 제한), 데이터는 "참고용"이며 정확한 혜택은 카드사 공식 확인이 필요함을 서비스 내 고지
- **데이터 정확성 책임**: 결과 화면에 "실제 카드사 조건과 다를 수 있습니다" 안내 문구 필수

---

## 10. 비기능 요구사항

- **플랫폼**: 데스크톱 웹 우선 (반응형은 v1 이후 보강)
- **성능**: 계산은 클라이언트 사이드에서 즉시 처리 (서버 왕복 최소화)
- **보안/개인정보**: 로그인 없음 → 개인정보 수집 최소화, 소비패턴 입력값은 로컬에만 저장
- **접근성**: 표 형태 결과의 가독성 확보 (스크린리더 고려는 필수는 아니나 시맨틱 마크업 권장)

---

## 11. 기술 스택 제안

- **프론트엔드**: Next.js (React) + TypeScript, 상태관리는 로컬 스토리지 연동이 단순하므로 React 기본 상태 + Zustand 정도로 충분
- **백엔드/데이터**: 카드 데이터는 별도 크롤링/수집 스크립트(Node.js 또는 Python) → DB(PostgreSQL 또는 초기엔 JSON 파일)로 관리 후 API Route(Next.js API) 또는 정적 JSON으로 제공
- **배포**: Vercel (Next.js 친화적, 특별한 선호 없다고 하셨으므로 기본 추천)
- **디자인 시스템**: 뱅크샐러드/토스 스타일 참고 — 단순하고 신뢰감 있는 금융 UI, 카드형 레이아웃, 명확한 데이터 시각화(표/색상 구분)

---

## 12. 저장/인증 전략 로드맵

- **v1**: 로그인 없이 로컬 스토리지 기반 저장 (기기별)
- **v2**: 계정(이메일/소셜 로그인) 도입, 서버 저장으로 기기 간 동기화 지원, 기존 로컬 데이터 마이그레이션 기능 고려

---

## 13. 향후 로드맵 (v2 이후)

- 계정 기반 저장 및 기기 간 동기화
- 카드 추천 순위 기능 (소비패턴 기반 미보유 카드 추천)
- 관리자 대시보드 (카드/혜택 데이터 CRUD, 크롤링 스케줄 관리)
- 자동 정기 크롤링/API 동기화
- 수익 모델 도입 검토 (카드 신청 제휴, 프리미엄 기능 등)
- 모바일 반응형/PWA 지원

---

## 14. 오픈 이슈 / 결정 필요 사항

- 서비스 최종 명칭 (현재 가칭 "카드혜택 계산기")
- 크롤링 대상 카드사 우선순위 및 순서
- 전월실적 "구간별 차등 조건" 등 예외 케이스의 데이터 표현 방식 세부 정의
- 카테고리 표준 목록 확정 (예: 외식/카페, 통신, 교통, 온라인쇼핑, 마트/편의점, 구독/OTT 등)
